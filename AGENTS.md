# Flowterm 协作补充说明

- 适用于仓库全部目录。

## 最近偏好补充

- UI 细节问题优先做最小范围修复；如果是终端排版不紧凑，先检查 `xterm` 的 `fontSize`、`lineHeight`、`letterSpacing` 等参数，避免无关重构。
- 每次修复完成后，回复里需要同步说明：问题原因、改动思路、修复方案、旧实现与新实现的关键差异。
- 如果执行全量验证失败，需要明确区分“本次改动问题”和“仓库已有问题”。
